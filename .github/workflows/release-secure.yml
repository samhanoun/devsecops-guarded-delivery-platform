name: release-secure

on:
  push:
    branches:
      - master
      - main
  workflow_dispatch:

jobs:
  release-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Release security scaffold check
        run: |
          echo "release-secure workflow is healthy"
          echo "next steps: wire syft/trivy/cosign"
